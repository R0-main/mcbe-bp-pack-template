import { EColors } from '../ui/colors/colors-enum';

const Config = {
	colors: {
		primary: EColors.Aqua,
		secondary: EColors.Blue,
		accent: EColors.MaterialDiamond,
	},
} as const;

export default Config;
